# LOAD-01H2 test scope

Implementation tests planned in `docs/hotfixes/LOAD-01H2-backpressure-safe-large-config-stream.md`.

This placeholder only keeps the hotfix branch explicit before runtime changes; it must be removed or replaced by executable tests before the PR is marked ready for review.
